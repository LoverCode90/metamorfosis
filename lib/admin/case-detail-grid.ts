/** Two-column case detail grids — wide primary + narrow sidebar. */
export const CASE_DETAIL_PRIMARY_GRID_CLASS =
  "grid gap-6 lg:grid-cols-[minmax(0,1.65fr)_minmax(0,1fr)]"

/** Equal split for conversation + evidence row. */
export const CASE_DETAIL_SPLIT_GRID_CLASS = "grid gap-6 lg:grid-cols-2"

/** Fixed height for chat and evidence panels so the row stays balanced. */
export const CASE_DETAIL_PANEL_HEIGHT_CLASS = "h-[min(520px,70dvh)]"
